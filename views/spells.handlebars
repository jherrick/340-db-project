
<h2>Spells</h2>

<table id="table" border="1">
   <thead>
     <th>Name</th>
     <th>Type</th>
   </thead>
   <tbody>
      {{#each spells}}
        <tr>
          <td>{{spellName}}</td>
          <td>{{spellType}}</td>
        </tr>
      {{/each}}
   </tbody>
</table>

<h2>Add Spell</h2>
<div>
  <form id="addspell" action="/spells" method="post">
     <label for="name">Name</label>
     <input type="text" name="name" placeholder="Expecto Patronum">

     <label for="Type">Type</label>
     <input type="text" name="type" placeholder="Hex">
    <input type="submit" value="Submit">
  </form>
</div>



<h2>Characters and their Spells</h2>
<table id="table" border="1">
   <thead>
     <th>Name</th>
     <th>Spell</th>
   </thead>
  <tbody>
    {{#each charspell}}
      <tr>
        <td>{{fname}} {{lname}}</td>
        <td>{{spell}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>

<h1>Add Characters' Spell</h1>
<div>
  <form id="addcharspell" action="spells/charspells" method="post">
   <label for="character">Character</label>
   <select name="characterId">
      {{#each characters}}
       <option value="{{characterId}}">{{fname}} {{lname}}</option>
      {{/each}}
   </select>

   <label for="spell">Spell</label>
   <select name="spellId">
      {{#each spells}}
      <option value="{{spellId}}">{{spellName}}</option>
      {{/each}}
   </select>
   <input type="submit" value="Submit">
</form>
</div>

<h1>Remove Characters' Spell</h1>
<div>
<form id="form">
   <label for="character">Character</label>
   <select name="character">
       <option value="1">Autopopulate</option>
   </select>

   <label for="character">Spell</label>
   <select name="spell">
       <option value="1">Autopopulate</option>
   </select>
</form>
<input id="submit" type="submit" value="Submit" onclick="submit()"/>
</div>